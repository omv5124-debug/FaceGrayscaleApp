* text=auto eol=lf

# Gradle wrapper jar must be tracked as binary to avoid corruption
gradle/wrapper/gradle-wrapper.jar binary

# Batch files need CRLF on Windows
gradlew.bat text eol=crlf

# Properties files must keep exact bytes (escape sequences, single backslashes)
gradle/wrapper/gradle-wrapper.properties text
